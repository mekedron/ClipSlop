---
sidebar_position: 6
title: Display modes
description: Plain text, Markdown, or HTML. Toggle the renderer with ⌘D and edit any result inline.
---

import useBaseUrl from '@docusaurus/useBaseUrl';

# Display modes

Results render as plain text, Markdown (native or HTML renderer), or HTML — toggle with <kbd>⌘D</kbd>.

| Mode                | When to use                                                   |
|---------------------|---------------------------------------------------------------|
| **Plain text**      | Default. Faithful to the model output, no parsing.           |
| **Markdown (native)** | Pretty rendering for any prompt that returns Markdown.       |
| **Markdown (HTML)**   | Same idea but uses an HTML renderer — better for complex tables. |
| **HTML**            | The model returned HTML directly (e.g., from the **Convert** group). |

## Editing

Hit <kbd>⌘E</kbd> to edit the result inline — the edit becomes its own history step:

<img
  src={useBaseUrl('/demos/screenshots/markdown-editor.png')}
  alt="Markdown editor inside the working area — inline editing of a transformation result"
  style={{maxWidth: '100%', borderRadius: 8, boxShadow: '0 6px 24px rgba(0,0,0,0.12)'}}
/>

When you save your edit (<kbd>⌘Enter</kbd> or click outside), it's added as a new step in the chain. Branch off it, run another prompt against your edited text, or copy it out.

## Per-prompt default

Each prompt can specify its preferred display mode in **Settings → Prompts**. The **Markdownify** prompt defaults to Markdown rendering; the **HTMLify** prompt defaults to HTML; everything else defaults to plain text. Override per prompt as needed.
