---
sidebar_position: 1
title: Inline Run
description: Bind any prompt to a global hotkey. Captures selected text, runs the prompt, pastes the result back — all without leaving your app.
---

import useBaseUrl from '@docusaurus/useBaseUrl';

# Inline Run

```
Select text → ⌃⌘G → grammar fixed in place
```

Inline Run is the fastest way to use ClipSlop. Assign a global hotkey to any prompt, and ClipSlop runs entirely in the background:

1. Captures the currently selected text.
2. Sends it through the prompt.
3. Pastes the result back, replacing the selection.

You never leave your app.

<video
  src={useBaseUrl('/demos/videos/inline-run.mp4')}
  controls
  autoPlay
  loop
  muted
  playsInline
  style={{maxWidth: '100%', width: '100%', borderRadius: 8, boxShadow: '0 6px 24px rgba(0,0,0,0.12)'}}
/>

Three lines of text, three different Inline Run shortcuts: <kbd>⌃⌘G</kbd> fixes grammar on the first line, <kbd>⌃⌘T</kbd> translates the second line to English, and <kbd>⌃⌘/</kbd> runs the one-shot prompt typed after `//` on the third. Every result pastes back in place.

## Default Inline Run shortcuts

A handful of prompts ship with a default global hotkey:

| Shortcut | Prompt              |
|----------|---------------------|
| `⌃⌘/`    | // Your prompt      |
| `⌃⌘G`    | Fix Grammar         |
| `⌃⌘F`    | Reformat            |
| `⌃⌘T`    | Translate → English |

Configure your own in **Settings → Prompts** — every prompt can have a Inline Run binding.

<Callout type="tip" title="Want to review the result first?">
  Use [Run in Editor](./open-and-run.mdx) instead — same idea, but ClipSlop opens its panel with the result so you can edit, branch, or keep chaining.
</Callout>

## How it works under the hood

ClipSlop simulates <kbd>⌘C</kbd> to read your selection, runs the prompt against your configured AI provider, then simulates <kbd>⌘V</kbd> to paste the result. The clipboard is restored to whatever was on it before the hotkey fired — Inline Run leaves no trace beyond the replaced selection.

## Want to bind a custom prompt?

Open **Settings → Prompts**, pick or create a prompt, and assign a **Global shortcut · Inline Run**. The shortcut works system-wide as long as ClipSlop has Accessibility permission.

See [Build a custom prompt](../guides/build-a-custom-prompt.mdx) for a step-by-step.
